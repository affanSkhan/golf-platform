"use client";
import {useState} from "react";
export default function Winner(){const[file,setFile]=useState<File|null>(null);const[submitted,setSubmitted]=useState(false);return <main className="min-h-screen bg-[#f4f0e7] px-6 py-12"><div className="mx-auto max-w-2xl"><a href="/dashboard" className="text-sm underline">← Dashboard</a><div className="py-16"><p className="text-xs uppercase tracking-[.3em] text-[#b56b38]">Winner verification</p><h1 className="mt-4 text-5xl font-semibold">Prove the score.</h1><p className="mt-5 text-black/60">Upload a screenshot of the scores from the golf platform. An administrator reviews the proof before payout moves from pending to paid.</p><div className="mt-10 rounded-3xl bg-white p-7"><label className="block text-sm font-medium">Score screenshot<input onChange={e=>setFile(e.target.files?.[0]??null)} className="mt-3 block w-full rounded-xl border p-3" type="file" accept="image/*,.pdf"/></label><div className="mt-5 rounded-2xl bg-[#f4f0e7] p-4 text-sm text-black/55">{file?"Selected file: "+file.name:"No file selected."}</div><button disabled={!file} onClick={()=>setSubmitted(true)} className="mt-5 w-full rounded-xl bg-[#10211d] p-3 text-white disabled:opacity-40">Submit proof</button>{submitted&&<div className="mt-5 rounded-2xl bg-[#dce5dc] p-4 text-sm">Submission recorded for admin review. Production storage is connected through Supabase in the next infrastructure step.</div>}</div></div></div></main>}