"use client";
import {useMemo,useState} from "react";
import {keepLatestFive,validateStablefordScore} from "../../lib/domain";
const starter=[{date:"2026-09-20",score:34},{date:"2026-09-13",score:37},{date:"2026-09-06",score:31}];
export default function Dashboard(){const[scores,setScores]=useState(starter);const[score,setScore]=useState("");const[date,setDate]=useState("");const ordered=useMemo(()=>keepLatestFive(scores),[scores]);function add(){const n=Number(score);if(!date||!validateStablefordScore(n)||scores.some(s=>s.date===date))return;setScores(keepLatestFive([...scores,{date,score:n}]));setScore("");setDate("");}return <main className="min-h-screen bg-[#f4f0e7] px-6 py-10"><div className="mx-auto max-w-6xl"><nav className="flex justify-between"><a href="/" className="font-semibold tracking-[.2em] text-sm">DIGITAL HEROES</a><a href="/admin" className="text-sm underline">Admin</a></nav><div className="grid gap-6 py-12 lg:grid-cols-[1.2fr_.8fr]"><section className="rounded-[2rem] bg-[#10211d] p-8 text-white"><p className="text-xs uppercase tracking-[.25em] text-white/50">Member dashboard</p><h1 className="mt-4 text-4xl font-semibold">Your game, your impact.</h1><div className="mt-8 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-white/8 p-5"><div className="text-white/45 text-sm">Subscription</div><div className="mt-2 font-semibold">Active</div></div><div className="rounded-2xl bg-white/8 p-5"><div className="text-white/45 text-sm">Charity</div><div className="mt-2 font-semibold">10%</div></div><div className="rounded-2xl bg-white/8 p-5"><div className="text-white/45 text-sm">Next draw</div><div className="mt-2 font-semibold">Monthly</div></div></div></section><section className="rounded-[2rem] bg-white p-8"><p className="text-xs uppercase tracking-[.25em] text-[#b56b38]">Add Stableford score</p><div className="mt-6 grid gap-3"><input value={date} onChange={e=>setDate(e.target.value)} className="rounded-xl border p-3" type="date"/><input value={score} onChange={e=>setScore(e.target.value)} className="rounded-xl border p-3" type="number" min="1" max="45" placeholder="1–45"/><button onClick={add} className="rounded-xl bg-[#10211d] p-3 text-white">Save score</button><p className="text-xs text-black/50">One score per date. The latest five are retained.</p></div></section></div><section className="rounded-[2rem] bg-white p-8"><div className="flex items-center justify-between"><h2 className="text-2xl font-semibold">Latest five scores</h2><span className="text-sm text-black/45">{ordered.length}/5 stored</span></div><div className="mt-6 grid gap-3">{ordered.map((s,i)=><div key={s.date} className="flex items-center justify-between rounded-2xl bg-[#f4f0e7] p-5"><div><div className="text-sm text-black/45">{s.date}</div><div className="mt-1 text-2xl font-semibold">{s.score}</div></div><div className="text-xs uppercase tracking-[.2em] text-black/40">{i===0?"Latest":"Stableford"}</div></div>)}</div></section></div></main>}