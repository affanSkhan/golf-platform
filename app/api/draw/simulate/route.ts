import {NextResponse} from "next/server";import {calculatePrizePools,simulateDraw,splitPrize} from "../../../../lib/draw";
export async function POST(req:Request){const body=await req.json().catch(()=>({}));const month=String(body.month||new Date().toISOString().slice(0,7));const candidates=Array.isArray(body.candidates)?body.candidates:[];const results=simulateDraw(candidates,month);const pool=calculatePrizePools(Number(body.subscriptionRevenue||0));const tiers={five:results.filter(x=>x.matchedNumbers===5),four:results.filter(x=>x.matchedNumbers===4),three:results.filter(x=>x.matchedNumbers===3)};return NextResponse.json({month,results,pools:pool,perWinner:{five:splitPrize(pool.five,tiers.five.length),four:splitPrize(pool.four,tiers.four.length),three:splitPrize(pool.three,tiers.three.length)}});}
