import {NextResponse} from "next/server";import Stripe from "stripe";import {getSupabaseServer} from "../../../lib/supabase/server";
export async function POST(req:Request){const secret=process.env.STRIPE_SECRET_KEY;const baseUrl=process.env.NEXT_PUBLIC_APP_URL;if(!secret||!baseUrl)return NextResponse.json({error:"payments_not_configured"},{status:503});const supabase=await getSupabaseServer();if(!supabase)return NextResponse.json({error:"database_not_configured"},{status:503});const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"unauthorized"},{status:401});const body=await req.json().catch(()=>({}));const plan=body.plan==="yearly"?"yearly":"monthly";const priceId=plan==="yearly"?process.env.STRIPE_YEARLY_PRICE_ID:process.env.STRIPE_MONTHLY_PRICE_ID;if(!priceId)return NextResponse.json({error:"price_not_configured"},{status:503});const stripe=new Stripe(secret);const session=await stripe.checkout.sessions.create({mode:"subscription",line_items:[{price:priceId,quantity:1}],customer_email:user.email??undefined,success_url:baseUrl+"/dashboard?checkout=success",cancel_url:baseUrl+"/pricing?checkout=cancelled",metadata:{user_id:user.id,plan}});return NextResponse.json({url:session.url})}
